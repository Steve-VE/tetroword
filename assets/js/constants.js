import { Vector } from "./classes/vector";

export const tileSize = 38;
export const gridSize = new Vector(10, 20);
export const gridHeight = tileSize * gridSize.y;
export const gridWidth = tileSize * gridSize.x;
